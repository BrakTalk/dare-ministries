node_modules/
_site/
.cache/
.DS_Store
# Local Netlify folder
.netlify
# Environment variables — never commit credentials
.env
.env.local
# Netlify CLI local artifacts
deno.lock
.gitnexus
# Local working documents (specs, guides, generated PDFs)
docs/
